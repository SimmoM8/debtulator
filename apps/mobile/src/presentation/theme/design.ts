// Compatibility export for existing feature modules. New shared UI should use
// `@/src/presentation/theme`; both paths resolve to the same canonical token objects.
export * from "@/src/presentation/theme";
