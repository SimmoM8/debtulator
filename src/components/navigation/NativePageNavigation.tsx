import type { NativePageNavigationProps } from "./types";

/** Native navigation is configured by the iOS implementation of this adapter. */
export function NativePageNavigation(_: NativePageNavigationProps) {
  return null;
}
