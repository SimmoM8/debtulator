import {
  debtulatorLightRoles,
  debtulatorPalette,
} from "@/src/theme/brand";

// Android continues to use its established React Native/Material presentation.
// These aliases make the shared brand source explicit without changing geometry.
export const androidBrand = debtulatorLightRoles;
export const ANDROID_APP_TINT = debtulatorPalette.primary;
